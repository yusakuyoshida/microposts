<!--
<%= form_for(current_user.favorites.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :favoritepost_id, @feed_item.id %></div>
  <%= f.submit "I like it", class: "btn btn-danger" %>
<% end %>
-->
<%= form_for(current_user.favorites.build(favoritepost_id: @feed_item.id)) do |f| %>
  <div><%= f.hidden_field :favoritepost_id %></div>
  <%= f.submit "I like it", class: "btn btn-danger" %>
<% end %>